# BGOBS 0.3.13

- Corrige le mode CaCam ADB sous Windows : les commandes ADB sont maintenant
  lancees sans fenetre `cmd.exe` visible.
- Corrige le quoting des chemins ADB contenant des espaces ou des caracteres
  speciaux, ce qui permet au forward `tcp:18080 -> tcp:8080` de se creer.
