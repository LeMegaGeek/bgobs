# BGOBS 0.3.14

- Synchronise les versions embarquees dans `manifest.json`, `plugin-support.c`,
  `VERSION` et `buildspec.json`.
- Reprend le correctif `0.3.13` du mode CaCam ADB sous Windows, sans fenetre
  `cmd.exe` visible et avec un quoting correct des chemins ADB.
